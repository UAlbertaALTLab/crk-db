/**
 * Aggregates all the data sources in an ALTLab database entry into the main entry.
 * @param  {Object} dbEntry
 * @return {Object} Returns the database entry, modified.
 */
export default function aggregateEntry(dbEntry) {

  return dbEntry;

}
